# Program.cs

Defines the logged-on user-session process boundary with a real cancellable lifetime. Clipboard correlation stays in the platform tracker; the service agent never reads clipboard state from Session 0 and no clipboard contents are retained.
