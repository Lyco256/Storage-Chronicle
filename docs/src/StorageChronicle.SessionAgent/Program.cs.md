# Program.cs

Defines the logged-on user-session process boundary. Clipboard notification integration is injected behind platform sources; the service agent never reads clipboard state from Session 0.
