# SessionAgentProtocol.cs

Defines clipboard candidate mapping and the bounded legacy session framing used by unit tests. Production maps candidates into the source-generated `ClipboardCandidateRequest` shared with the Agent and retains no clipboard payload bytes.
