# DesktopProjectionAdapters

Adapts the Agent IPC page contract to Event Stack's richer UI query model without importing storage or Windows APIs. It preserves bounded pages and does not invent detail rows when a detail IPC contract is unavailable.
