# UiContracts.cs

Defines UI feature registration, semantic icon resolution, virtualized page loading, and reconciliation prompt contracts. No storage or Windows API is referenced. Headless UI tests use fake projections.
