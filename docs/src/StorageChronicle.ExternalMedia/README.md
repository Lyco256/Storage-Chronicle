# External Media

This project owns media identity, mount sessions, immutable mirror segments, manifest recovery, and history branches. It depends on Domain and Contracts and does not access Windows collector APIs.
