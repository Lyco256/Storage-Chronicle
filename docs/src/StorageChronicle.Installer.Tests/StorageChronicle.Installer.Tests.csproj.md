# StorageChronicle.Installer.Tests.csproj

Test project for deterministic installer manifest checks. Physical MSI install, repair, and rollback remain separate Windows packaging tests driven by `Build-Installer.ps1`.
