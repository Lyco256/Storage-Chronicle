# StateAndProjection.cs

Defines immutable state snapshots, diff entries, Event Stack rows, projection modes, and bounded pages. These are OS-neutral read models; they do not perform I/O. State and projection tests validate path reconstruction, diff semantics, and paging.
