# LogInspector/Program.cs

Reads a bounded line stream and emits only severity counts. It supports `--json` and `--max-lines`, deliberately does not print or persist log contents, and reports truncation explicitly.
