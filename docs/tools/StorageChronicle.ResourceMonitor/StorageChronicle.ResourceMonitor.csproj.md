# StorageChronicle.ResourceMonitor.csproj

A net10.0, dependency-free console tool used by `Test-ResourceBudget.ps1`. It is intentionally platform-neutral so the harness can be built on non-Windows hosts while the process under test may be Windows-specific.
