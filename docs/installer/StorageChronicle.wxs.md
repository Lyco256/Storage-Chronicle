# StorageChronicle.wxs

Defines one MSI feature containing UI, Agent, and Session Agent. It preserves the machine-wide history directory on uninstall and configures service recovery without installing a filesystem driver.
