# StorageChronicle.wixproj

WiX Toolset SDK 6.0.2 packaging project for the per-machine x64 installation. The MSI owns the Agent Service registration; publish outputs are supplied by `build/package/Build-Installer.ps1`.
