# Test-Performance.ps1

Runs the BenchmarkDotNet suite with the one-million and one-hundred-thousand workloads and stores JSON/Markdown reports under `artifacts/benchmarks`. Benchmark output is diagnostic evidence, not an acceptance claim for release hardware.
