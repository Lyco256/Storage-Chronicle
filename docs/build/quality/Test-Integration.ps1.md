# Test-Integration.ps1

Resolves and runs the integration, Golden/E2E, and Avalonia headless xUnit v3 Microsoft Testing Platform modules and writes XML results under `artifacts/quality/integration`. It does not collapse privileged Windows tests into the default gate.
