# ADR 006: Immutable external-media segments and branches

Media mirrors use immutable segments, checksums, A/B manifests, and writer-specific branches. A shared parent with multiple valid children is represented as a history branch.
